# UART-PROTOCOL
UART is a simple, widely used serial communication protocol that allows two devices to exchange data without a shared clock. It works asynchronously, meaning both sender and receiver agree on a baud rate (bits per second) instead of using a clock line.
